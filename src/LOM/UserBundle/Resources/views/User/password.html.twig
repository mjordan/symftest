{% extends '::base.html.twig' %}

{% block body -%}
<h1>User edit</h1>

    {{ form(password_form) }}

<ul class="record_actions">
    <li>
        <a href="{{ path('user') }}">
            Back
        </a>
    </li>
</ul>

{% endblock %}
