Hello {{ user.fullname }}.

Your password reset code is {{ reset_code }} and it expires at
{{ user.getResetExpires }}.

Visit this link to reset your password:

{{ url('password_confirm', {'username' : user.username, 'resetcode' : reset_code } ) }}
